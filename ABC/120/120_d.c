#include <stdio.h>
int main(void)
{
    int i, f[100000];
    int N, M, a[100000], b[100000];
    scanf("%d%d", &N, &M);
    for (i = 0; i < M; i++)
    {
        scanf("%d%d", &a[i], &b[i]);
    }

    return 0;
}